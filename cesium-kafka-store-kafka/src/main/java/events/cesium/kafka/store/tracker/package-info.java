/**
 * The tracker-topic-backed scheduler store: tracker wire format, replay rules, cursor/sidecar
 * codec, and the fastutil-backed per-partition in-memory index.
 */
@org.jspecify.annotations.NullMarked
package events.cesium.kafka.store.tracker;
