/**
 * Ingest and dispatch policies (design §2.3, §7, D3) and the pure decision engine combining
 * header-codec results with the configured policies into final ingest actions.
 */
@org.jspecify.annotations.NullMarked
package events.cesium.kafka.core.policy;
